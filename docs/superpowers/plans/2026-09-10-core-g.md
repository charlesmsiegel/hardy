# Core G implementation plan

The roadmap is the approved design. Core D and F are verified on main. Core G
started in an independent worktree during F's final test gate; F's completed
documentation has been integrated before G's final gate.
All Core E items remain deferred by user instruction.

- [x] G0: extend Referee with bounded citation depth 0/1/2. Named source-bound
  expansion supplies child uses; existing C2 and ledger policy retain authority.
  Keep direct acceptance distinct from recursive coverage. Report cycles,
  unavailable sources, depth/node cutoffs and unresolved hypotheses explicitly.
- [x] G1: add pure manuscript version differencing plus an explicit semantic
  version-audit workflow. Mechanical source matching never certifies equivalence.
  Preserve mathematical evidence for unchanged items even when their source
  spans move; reopen affected current work through the existing reverse closure.
- [x] G2: expand section/chapter/book roots locally in the existing publication
  planner and pass structure to the existing assembler. Add BOOK to the current
  item-kind enum. CONTAINS remains outside mathematical dependencies.
- [x] G3: refresh only explicitly requested stale exposition. A named prose
  operation returns text; one revision-checked append updates the prose and its
  logical DOCUMENTS relation. Preserve old text and all mathematical records.

## Ownership and acceptance

G0 owns Referee and its citation helper. G1 owns literature/diff.py and a version
workflow. G2 owns publication.py, publish.py and the BOOK enum addition. G3 owns
a new exposition workflow. Root owns shared-policy decisions, commits and docs.
Use exact source/claim/context/scope identities and existing named operations;
no domain-specific math parsing or automatic weakening of statements.

Fixtures cover cyclic citation chains, foreign scopes, stale expansion results,
moved unchanged statements, changed hypotheses/notation, removed statements,
shared chapter dependencies, hidden helpers, minimal local contexts, containment
cycles and one explicit paragraph refresh amid unrelated stale prose.

Each item gets a tested commit with focused Ruff. Independently review seams,
then update README, DESIGN, FEATURES, ARCHITECTURE, roadmap and TODO together.
Freeze source for repository Ruff, the full hermetic coverage suite and relevant
wheel smoke checks before landing. Never relax tests or coverage to land:

`uv run --extra test pytest -q -m 'not real_toolchain and not live' --cov`

Semantic readers in fixtures are scripted and attributed. Neither lexical diff,
model review nor successful document compilation establishes mathematics.
