# Core A: contracts and shared primitives

> **For agentic workers:** Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task by task.

**Goal:** Complete roadmap A0–A5 on `implementation/a-contracts-and-shared-primitives`, based on `architecture/project-ledger-roadmap`, before creating Core B's branch.

**Spec:** `docs/research-architecture.md` and Core A in `docs/roadmap.md`.

**Architecture:** Project semantics live in immutable, transport-independent ledger values. Shared workflow operations consume capability results; interactive adapters retain confirmation and transcript duties. Literature parsing describes source structure without executing TeX or certifying mathematics.

**Stack:** Python 3.11+, existing Pydantic v2 `FrozenModel`, pytest, existing Lean/faithfulness/source primitives. No new dependencies or live model/toolchain calls.

## Global constraints

- Every implementation commit names the roadmap ID and the concrete aspect completed; documentation and test evidence accompany that aspect.
- Keep source and corpus changes separate. No corpus edits, main merges, archive tags, or resurrected prototypes.
- Reuse canonical owners. Capability code cannot import workflow controllers; the ledger cannot import provider transports.
- A model proposal is not formal evidence. Preserve original statements, exact source identities, explicit gaps, and audited assumption distinctions.
- Context hypotheses and conjectures never widen global trust. No execution isolation is added.
- Run focused tests for each task, then `uv run --extra test pytest --cov -m 'not real_toolchain and not live'` for the wave (this machine has installed real toolchains). Record external-environment limitations accurately.
- Later waves branch from the verified predecessor. A0 contracts precede Core B; Core A does not claim to implement B4/B5 resolution engines or sophisticated strategies.

## Task 1: A0 — immutable ledger contracts

Files: create `src/hardy/workflows/ledger/__init__.py`, `contracts.py`, and `tests/unit/test_ledger_contracts.py`.

Expose `ProjectItem`, `ProjectItemKind`, `ProjectOrigin`, `MathematicalContext`, `ScopedBinding`, `Obligation`, `ObligationKind`, `ObligationStatus`, `Relation`, `RelationKind`, `Scope`, `Resolution`, `EvidenceRef`, `ArtifactRef`, and `CitationContract`, with publication roles/visibility and generic declaration/research state. Prefer frozen tuple-based values, explicit version identities, and no domain-specific mathematical enums. No storage, providers, or UI dependencies.

- [x] Write acceptance fixtures covering theorem→lemma→definition, illustration/documentation at exact digests, external-result versus must-prove scope, multiple representations, immutable parent contexts, aliases, unproved conjectures, blocked approaches, and outstanding transport justification. Reject malformed identities, inconsistent declarations and inadmissible resolution records.
- [x] Run `uv run --extra test pytest tests/unit/test_ledger_contracts.py -q` before implementation, record missing behavior, then implement validated contracts.
- [x] Repeat the focused tests and review the contracts against A0's fixture. Contract validation must not pretend to establish mathematical truth; evidence policy is implemented in B2.
- [x] Update A0's roadmap status and commit `feat(A0): define immutable project ledger contracts`.

## Task 2: A1 — enforce new import boundaries

Files: extend `tests/unit/test_module_boundaries.py` using its existing transitive import graph.

- [x] Add a pure boundary-query helper over the existing graph and synthetic graph fixtures that detect direct and transitive provider/controller dependencies, including package imports.
- [x] Allow shared workflow primitives to consume capability contracts while preventing ledger imports of model transports, application assembly, or execution controllers.
- [x] Run `uv run --extra test pytest tests/unit/test_module_boundaries.py -q`; verify the negative fixtures fail if the new restriction is removed.
- [x] Update A1's status and commit `test(A1): enforce shared workflow and ledger boundaries`.

## Task 3: A2 — shared statement formalization

Files: create `src/hardy/workflows/formalization.py` and `tests/unit/test_formalization.py`;
integrate `src/hardy/workflows/prove.py`, `src/hardy/formal/contracts.py`,
`src/hardy/formal/verifier.py`, `src/hardy/app/mcp.py`, and
`src/hardy/prompts/__init__.py`; update `docs/roadmap.md` and this plan.
Retain existing staged/faithfulness tests. Execution evidence is in
`.superpowers/sdd/2026-09-09-core-a/task-3-report.md`.

Expose a reusable operation/service for proposal construction, Lean elaboration, freezing and independent faithfulness review, without a `MathematicsSession`. Preserve Prove's approval, cancellation, budget and persisted-artifact sequencing. Semantic input carries exact context/item references and declaration-to-binder mappings; unresolved representation/declaration/transport requirements return typed obligations. Core B supplies resolution later, so unresolved semantic inputs must fail explicitly rather than silently fall back to empty context.

- [x] Characterize Prove's current approval/revision/refusal/cancellation sequence; add direct service tests for faithful/unfaithful/unavailable readers and semantic blockers.
- [x] Extract from the existing path, reuse `freeze_claim`, Lean checking and `review_translation`; have Prove call the shared operation without moving UI ownership.
- [x] Run `uv run --extra test pytest tests/unit/test_formalization.py tests/unit/test_faithfulness.py tests/tui/test_prove_command.py tests/unit/test_workflow.py tests/unit/test_workflow_modulo.py -q`.
- [x] Update A2 and document the B4/B5 integration boundary. Commit `refactor(A2): share statement formalization and review`.

Controller rulings: Prove's public request schema remains standalone. Contextual
service callers exercise full persisted-readback, reader and verifier/MCP paths;
project-aware Prove requests belong to later B/D integration. Caller/B5 supplies
resolved exact records and required declaration origins, while the provider generates
binder fragments. Several fragments may have the same origin. Raw binders are derived
exclusively from those fragments. Source text is derived from supplied records; formal
contracts never import the ledger. Selected sources and their explicit dependency/alias
references must be complete, while unrelated context declarations may stay unmaterialized.
Full closure discovery, justification acceptance and representation adequacy remain B4/B5.

Validation: the required Task 3 command passed **144 tests**. The additional
`uv run --extra test pytest tests/unit/test_verifier.py tests/unit/test_mcp_server.py tests/unit/test_module_boundaries.py tests/unit/test_verified_modulo.py tests/unit/test_ledger_contracts.py -q`
passed **96 tests**. Thirty direct service tests include legacy hash/schema
compatibility, contextual persistence, actual verifier/MCP reconstruction, independent
reader outcomes and forged/missing binder origins. These checks use fake tool/model
responses; they make no claim about a real Lean encoding or model translation quality.

## Task 4: A3 — generic assumption admission policy

Files: create `src/hardy/workflows/admission.py`, refactor the interactive admission
adapter and required session wrappers, route CLI structural admission and
`ProveWorkflow._refute` through the shared owner, and add direct policy tests with
existing assumption regressions. Prove retains the lifecycle established in A2.

Extract the existing search-first, shape/elaboration, probe, source/faithfulness and scope decisions into a session-free owner. Keep human confirmation, event/persistence and publication adapters interactive. Use explicit trust-request categories rather than guessing from prose. A local binder/hypothesis or conjecture cannot become a globally admitted assumption. B2 later supplies ledger scope policy through the same seam.

- [x] Write direct policy tests for exhausted search evidence, malformed/provable/refuted statements, unread/disagreed sources, target self-assumption, and excluded local context/conjectures.
- [x] Extract policy while preserving current search consumption, quarantine versus unavailable-reader behavior and rollback.
- [x] Run the direct policy tests and all assumption/paper-admission regression tests.
- [x] Update A3 and commit `refactor(A3): centralize assumption admission policy`.

Controller rulings: actual probe construction, stripping, diagnostic interpretation
and sequencing live in the generic owner over named Lean operations. Complete
source versus staged header ownership and the axiom-last probe layout stay fixed.
Subject/scope are paired; exact `must_prove` self-assumption and stale revisions
sharing its stable ID both refuse. CLI declarations are caller-preauthorized,
with structural validation and refutation in their existing stages; no search,
source reader or new confirmation is added. Research conjecture creation is
excluded, while an explicit paper-assumption request still traverses its gates
regardless of the paper's inventory kind. B2 authenticates ledger evidence later.

Validation: 330 focused tests passed on 2026-09-09, covering direct policy,
assumption/search/paper/refutation/CLI/modulo behavior, interactive-owner rollback,
and module/architecture boundaries. The final command used the eleven files in
the task brief plus `test_interactive_owners.py` and `test_architecture_doc.py`,
with `-q -m 'not real_toolchain and not live'`. The Windows baseline's unrelated
failures remain a wave-level concern; no full-suite or live result is claimed here.

## Task 5: A4 — proof-strategy contract

Files: create `src/hardy/workflows/strategies/__init__.py`, `contracts.py`, and `tests/unit/test_strategy_contracts.py`.

Expose `ProofTask`, `ProofOutcome`, and a `Strategy` protocol. Reuse `FrozenClaim`, `DeclaredAssumption`, verification and budget contracts. Strategy output describes an attempt/submission and cannot independently assign a kernel grade. Validate nonnegative finite budgets and exact task/evidence linkage where evidence is accepted.

- [x] Test partial/cancelled/exhausted attempts and matching versus mismatched statement identity, plus a small deterministic fake strategy consuming the protocol.
- [x] Implement only the interface and its value invariants, no search engine.
- [x] Run `uv run --extra test pytest tests/unit/test_strategy_contracts.py -q` (13 passed).
- [x] Update A4 and commit `feat(A4): define bounded proof strategy contracts`.

Validation: the focused A4 suite passed 13 tests. The A4 suite plus the
existing import-boundary suite passed 40 tests with
`-m 'not real_toolchain and not live'`. `ProofTask` carries the frozen claim,
explicit declared-assumption scope, and existing `RunLimits`; it validates its
active/proof/check/process/retrieval ceilings without changing legacy callers.
`ProofOutcome` remains descriptive rather than graded. If it carries existing
verification evidence, the evidence must match the exact canonical verifier
source constructed from this task, its declaration scope, and its returned
proof. C5 supplies iterative execution later; A4 adds no search, provider,
verification execution, or token/cost accounting.

## Task 6: A5 — mechanical manuscript structure

Files: create `src/hardy/literature/manuscript.py`, `tests/unit/test_manuscript.py`; reuse existing `literature/statements.py` and `documents/syntax.py` where their semantics match.

Expose immutable source spans and a mechanical inventory of sections, theorem/definition/proof environments, labels, and citation occurrences over an explicit source mapping. Offsets reference original text and carry source digest; duplicate occurrences remain distinct. Do not infer semantic claim boundaries, expand arbitrary macros, execute TeX, or read outside the supplied mapping. Report unparsed/unsupported structure honestly.

- [x] Write fixtures with nested environments, comments, escaped percent, repeated labels/cites, multiline commands, malformed endings and source identity changes.
- [x] Implement the smallest scanner using existing parsing behavior where possible; test exact original source slices and determinism.
- [x] Run `uv run --extra test pytest tests/unit/test_manuscript.py -q` and existing literature inventory tests.
- [x] Update A5 and commit `feat(A5): inventory mechanical manuscript source structure`.

## Wave verification and pause

- [x] Reconcile README, DESIGN, FEATURES and ARCHITECTURE with completed A0-A5 primitives and known integration limits; update the roadmap's current execution-scope note. Focused evidence is task-specific rather than an invented aggregate: A0 `test_ledger_contracts.py` (21 passed), A1 `test_module_boundaries.py` (26 passed), A2's required workflow command (144 passed) plus consumer/boundary command (96 passed), A3's admission regression command (330 passed), A4's final contract/boundary/architecture command (51 passed), and A5's manuscript/assume/boundary/architecture command (68 passed). Re-run the six primary surfaces with `uv run --extra test pytest tests/unit/test_ledger_contracts.py -q`; `uv run --extra test pytest tests/unit/test_module_boundaries.py -q`; `uv run --extra test pytest tests/unit/test_formalization.py -q`; `uv run --extra test pytest tests/unit/test_admission_policy.py -q`; `uv run --extra test pytest tests/unit/test_strategy_contracts.py -q`; and `uv run --extra test pytest tests/unit/test_manuscript.py -q`.
- [ ] Obtain an independent whole-wave review and run focused tests for any concrete findings before proceeding. Full hermetic coverage is deferred at the user's request; do not claim a full-suite green result or coverage.
- [ ] Verify the completed A branch contains architecture planning history and task-specific commits, confirm its final working-tree state, then pause. Do not create a B branch or begin Wave B.
