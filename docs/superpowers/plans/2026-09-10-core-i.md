# Core I implementation and integration plan

Core I follows the verified G/H work and keeps all of Core E deferred. X2
transcript durability is a prerequisite implemented in this branch.

- [x] X2: checkpoint independent in-flight blocks and preserve torn-tail bytes.
- [x] I0: persist transcript IDs/parents and branch epochs; explicit fork/abandon
  replay the selected visible history without rolling back mathematical state
  or spend. Refuse active/stale workers; keep human lessons unverified.
- [x] I1: use the existing template and transcript path for generic mathematical
  shortcuts, with project overrides and no new evidence/approval authority.
- [x] I2: retain configured model identities and make curated catalog provenance,
  unknown capabilities and unverified availability explicit.

Each item received related tests and an individual commit. Integrate G/H, run
the full hermetic coverage gate on frozen source, verify lint and a fresh
installed wheel, then update the roadmap/TODO and land only if clean.
