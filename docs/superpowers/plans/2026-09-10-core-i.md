# Core I and transcript durability plan

The approved roadmap and user direction allow work independent of Core E.
This worktree starts from verified Core F; integrate G/H before its landing gate.

- [ ] X2: checkpoint partial assistant blocks at a bounded interval, record
  in-flight tool calls, and prefer completed blocks over their checkpoints on
  replay. Preserve transcript identity, crash recovery and honest usage totals.
- [ ] I0: add transcript entry IDs/parents, active leaf, branch/fork/abandon and
  branch summaries. Reuse the durable transcript. Conversation branches are
  separate from mathematical context, ledger approaches and proof workers.
- [ ] I1: add generic project command/template expansion. Expanded text is
  recorded user input, never evidence or implicit permission to widen trust.
- [ ] I2: improve backend-aware model catalog discovery and presentation using
  existing configuration and provider capabilities. Verify current external
  catalog behavior from primary sources if an adapter requires it.

Use existing stream, record and terminal owners. Reproduce failure cases before
fixing behavior, and test each item before its own commit. Review interruption,
restart, branch provenance, completion precedence and concurrent prompt behavior.
Root owns commits and integration. Update the roadmap, TODO and four overview
documents, then freeze source for repository Ruff, full hermetic coverage and
wheel smoke before landing. Core E remains unchecked and unimplemented.
