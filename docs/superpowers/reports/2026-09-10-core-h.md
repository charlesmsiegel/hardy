# Core H verification report

H0, H1 and H2 are implemented as shared retrieval and evaluation APIs. Core E
remains deferred for human input. Each item received a tested commit.

| Item | Commit | Verification before commit |
| --- | --- | --- |
| H0 scoped authenticated retrieval | `7a27c77` | 116 related tests, Ruff |
| H0 payload/evidence review corrections | `ca99806` | 268 related tests, Ruff |
| H1 durable-reuse measurement | `fdf81c2` | 32 retrieval/reuse tests |
| H2 exposure identity and audit | `0fd3b97` | 268 related tests, Ruff; independent 29-case exposure review |

H0 derives a rebuildable index with exact source identity, scoped aliases,
context members, goals and explicitly historical approach assessments. Delivery
reauthenticates current ledger policy, scope/context, formal artifacts,
environment and importability. Shared-library records require an explicit
project authorization bridge. Missing stores remain distinct from empty stores.
The complete accepted payload is bounded; rejected diagnostics do not consume
that budget. Review regressions cover oversized historical metadata and proof
revocation caused during later capability reads.

H1's [fixture report](2026-09-10-project-reuse.md) records six category expectations
recovered after ledger/index restart with retrieval enabled, versus none without.
The goal and approach share a query. Capability readers are scripted, and no
provider or Lean process is called. This supports retaining existing stores for
these cases; it establishes no live-model gain or universal memory conclusion.

H2 binds the frozen source/index/query/split plan to run identity, and binds
owner-written exposure receipts to actual forwarded provider input and trajectory
events. Exact repeat, related transfer, declared local held-out and unknown
exposure remain separate. Missing or truncated coverage cannot establish held-out
performance. Review regressions reject detached delivery files, relabelled run
identities and stripped exposure fields. Local completeness says nothing about
provider pretraining.

## Final landing gate

The complete hermetic gate passed on frozen source `0fd3b97`:
**4,338 passed, 159 skipped, 36 deselected; 90.04% coverage** (82% required),
in **602.13 seconds**. Later edits finalize documentation and portable source-hash
notation for the measurement report.

```powershell
uv run --extra test pytest -q -m 'not real_toolchain and not live' --cov --cov-report=xml --cov-report=html --cov-report=term --tb=short
```

Repository Ruff, wheel build and fresh installed-wheel smoke passed under Python
3.13.11. The smoke exercised packaged assets, CLI help, deterministic runs, the
CAS helper and MCP stdio outside the checkout. The hermetic environment uses
locked dependencies; wheel installation resolves declared dependency ranges.
Wheel environment: `%TEMP%/hardy-core-h-wheel-21d30a65fa9f4e1aa24e03609f2a0263`.
Full-suite log: `%TEMP%/hardy-core-h-final-hermetic.log`.
