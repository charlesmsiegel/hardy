# Core I and X2 verification report

Core I and its X2 durability prerequisite are implemented. Core E remains
deferred. Every item received its own tested commit.

| Item | Commit | Verification before commit |
| --- | --- | --- |
| X2 independent streaming checkpoints and torn tails | `d01943c` | 181 passed, 3 skipped; independent review and Ruff |
| I0 durable conversation branches | `c992a4f` | 370 passed, 3 skipped; independent 48 branch/boundary tests |
| I1 recorded prompt shortcuts | `4608ceb` | 486 passed, 3 skipped; independent 26 command tests |
| I2 honest catalog and configured choices | `d5fe6c7` | 452 passed, 1 skipped; independent 42 catalog/model tests |
| G/H integration | `cc35feb` | 79 integration tests and repository Ruff |

X2 checkpoints each in-flight block independently, synchronizes durable appends
and preserves interrupted/torn bytes without combining them with the next event.
I0 adds stable IDs, parent links, a durable branch cursor/epoch and cross-process
append checks. Forks construct fresh runtimes using the exact selected visible
history; they do not infer opaque provider state. Replay stays bounded to 1 MiB,
and partial checkpoints remain partial. Stale writers/threads and active workers
are refused. Compaction and export follow the selected branch, while spend counts
all branches. Mathematical scopes, assumptions and saved artifacts remain current.
Abandonment lessons are attributed human text, not proof evidence.

I1 reuses existing template expansion and transcript dispatch. Project prompts
override the four bundled shortcuts; operational commands remain reserved. A
`/publish` request prepares a local draft and does not implement deferred Core E
publication wiring. I2 retains configured identities, including incompatible or
unlisted ones, and labels curated provenance and unverified availability. It
adds no model identities, live discovery API or unverified capability claims.

## Final landing gate

Frozen source `cc35feb` passed the full hermetic gate:
**4,397 passed, 159 skipped, 36 deselected; 90.09% coverage** (82% required),
in **625.83 seconds**. Later changes only finalize documentation.

```powershell
uv run --extra test pytest -q -m 'not real_toolchain and not live' --cov --cov-report=xml --cov-report=html --cov-report=term --tb=short
```

Repository Ruff, wheel build and fresh installed-wheel smoke passed under Python
3.13.11. The wheel smoke exercised assets, CLI help, deterministic runs, the CAS
helper and MCP stdio outside the checkout. Hermetic tests use locked dependencies;
wheel installation resolves declared dependency ranges.
Wheel environment: `%TEMP%/hardy-core-i-wheel-3c3b825b085b4cc7abb647ebddc11fb6`.
Full-suite log: `%TEMP%/hardy-core-i-final-hermetic.log`.
